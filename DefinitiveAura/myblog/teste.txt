<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Blog Aura</title>
    <link rel="stylesheet" href="css/style.css">
</head>
<body>
    <header class="header">
        <div class="logo-container">
            <img src="images/logoa.png" alt="Logo Aura">
        </div>
    </header>

    <div class="container">
        <!-- Destaque de 2 posts -->
        <section class="featured-posts">
            <?php
            // Seleciona os dois primeiros posts
            $sql_query = $conn->query("SELECT * FROM posts WHERE status='pending' ORDER BY created_at DESC LIMIT 2");
            $ponto = '.';
            if ($sql_query->num_rows != 0) {
                while ($post = $sql_query->fetch_assoc()) {
                    echo "<div class='post-card'>";
                    // Exibir imagem se houver
                    if (!empty($post['full_path'])) {
                        echo "<img src='uploads/" . $post['nome'] . $ponto . $post['formato'] . "' alt='Imagem do Post'>";
                    }
                    echo "<div class='post-content'>";
                    echo "<h2><a href='post.php?id=" . $post['id'] . "'>" . htmlspecialchars($post['title']) . "</a></h2>";
                    echo "<p>" . substr(htmlspecialchars($post['content']), 0, 100) . "...</p>";
                    echo "</div>";
                    echo "</div>";
                }
            }
            ?>
        </section>

        <!-- Lista de posts sem imagem -->
        <section class="other-posts">
            <h2>Outros Posts</h2>
            <div class="post-list">
                <?php
                // Seleciona os demais posts
                $sql_query = $conn->query("SELECT * FROM posts WHERE status='pending' ORDER BY created_at DESC LIMIT 2, 999");
                if ($sql_query->num_rows != 0) {
                    while ($post = $sql_query->fetch_assoc()) {
                        echo "<div class='post-item'>";
                        echo "<h3><a href='post.php?id=" . $post['id'] . "'>" . htmlspecialchars($post['title']) . "</a></h3>";
                        echo "<p>" . substr(htmlspecialchars($post['content']), 0, 150) . "...</p>";
                        echo "</div>";
                    }
                } else {
                    echo "<p>Sem posts disponíveis.</p>";
                }
                ?>
            </div>
        </section>
    </div>

    <?php include('includes/footer.php'); ?>
</body>